@using Blog.Blazor.Models.hexo
@using Blog.Blazor.Services
<h3>ArticleList</h3>


<table class="table">
    <thead>
        <tr>
            <th>Title     </th>
            <th>Tags      </th>
            <th>Updatedtime  </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var post in posts)
        {
            <tr>
                <td>@(post.Title)      </td>
                <td>
                    @foreach (var tag in new string[]{"#tag1","#tag2"})
                        {
                        <Tag>@tag</Tag>
                    }
                </td>
                <td>@(post.Updated)   </td>
            </tr>
        }
    </tbody>
</table>

@inject HexoService hexo
@code {
    public IList<Post> posts;

    protected override void OnInitialized()
    {
        posts = hexo.GetAllPosts().Where(e=>!string.IsNullOrWhiteSpace(e.Title)).ToList();
    }
}
